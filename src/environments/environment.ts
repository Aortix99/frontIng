export const environment = {
  production: false,
  /** Backend local (`npm run dev` en backIng). Para producción usa environment.prod.ts */
  apiUrl: 'https://backing-iwik.onrender.com/api'
  // apiUrl: 'http://localhost:5000/api',
};
